describe('Adds numbers', () => {
  it('Add 5 + 5 equal 10', () => {
    expect(5 + 5).toEqual(10);
  });
});

describe('Adds numbers', () => {
  it('Add 5 + 5 to equal 10', () => {
    expect(5 + 5).toEqual(10);
  });
});
